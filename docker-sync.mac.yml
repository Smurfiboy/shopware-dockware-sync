version: "2"
syncs:
  shopware-volume:
    sync_strategy: 'unison'
    notify_terminal: true
    src: '.'
    sync_excludes: ['var/cache', '.docker-sync', '.DS_Store', 'docker-compose.yml', 'docker-sync.yml']
